class HelloWorld {
    public static void main(String[] args) {
        int[] arr={1,2,3,4};
        
        double sum=0;
        int size=arr.length;
        for(int i=0;i<size;i++){
            sum=sum+arr[i];
        }
        double avg=sum/size;
        System.out.println(avg);
    }
}
